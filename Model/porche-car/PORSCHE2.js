// Function to speak the text
function speakText(text) {
    const speech = new SpeechSynthesisUtterance();
    speech.text = text;
    speech.pitch = 1;
    speech.rate = 1;
    speech.volume = 1;

    // Set the voice to the first available voice (you can change the voice if needed)
    speechSynthesis.speak(speech);
}

document.getElementById('function').addEventListener('click', () => {
    const text = "The Porsche 911 is two- door 2+2 high performance rear- engined sports car introduced in September 1964 by PorcheAG of Stuttgart  , Germany.       It has rear-mounted flat-six engines and originally a torsion bar suspension.      The car has been continuously enhanced through the years but the basic concept has remained unchanged.          The engines wereair-cooled until the introduction of the 996 series in 1998.        The 911 is used by private and factory teams for racing and automotive competition.     It is the most successful competition cars.     The Porsche 911 is a high-performance sports car with many sophisticated component is a sports car which is highly in demand and is one of the two top five best cars till now.     Porsche vehicles are known for their high performance, luxury, and advanced technology";
    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});



// Handling the button click event
document.getElementById('button1').addEventListener('click', () => {
    const text = "Sports pedals made of metal for better grip and control.";

    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button2').addEventListener('click', () => {
    const text = 'Includes the hood, doors, fenders, roof, and rear panels.'
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button3').addEventListener('click', () => {
    const text = 'LED or Xenon headlights, often iconic round Porsche design.'
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button4').addEventListener('click', () => {
    const text = 'Modern LED tail lights with the signature 911 shape.'
    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button5').addEventListener('click', () => {
    const text ='Aerodynamically shaped side mirrors'
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button6').addEventListener('click', () => {
    const text = " Active or fixed rear spoilers for aerodynamics."

    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button7').addEventListener('click', () => {
    const text = "Twin or quad exhaust pipes, depending on the model."

    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button8').addEventListener('click', () => {
    const text = "Made of lightweight materials like aluminum or high-strength steel."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button9').addEventListener('click', () => {
    const text = "Multi-link rear suspension, with MacPherson strut at the front. Some models include adaptive dampers."

    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button10').addEventListener('click', () => {
    const text = "High-performance disc brakes with carbon-ceramic options."
    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button11').addEventListener('click', () => {
    const text = " Electric power steering with options like rear-axle steering."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button12').addEventListener('click', () => {
    const text = "Turbocharged flat-six (boxer) engine, often mounted in the rear."

    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button13').addEventListener('click', () => {
    const text = "Available in manual or Porsche’s PDK dual-clutch automatic transmission."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button14').addEventListener('click', () => {
    const text = "Rear-wheel drive (RWD) or all-wheel drive (AWD) depending on the model."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button15').addEventListener('click', () => {
    const text = "Many models are equipped with twin-turbochargers."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button16').addEventListener('click', () => {
    const text = "Air-to-water intercoolers, radiators, and oil coolers."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button17').addEventListener('click', () => {
    const text = "Digital and analog gauges, including the iconic central tachometer."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button18').addEventListener('click', () => {
    const text = "Porsche Communication Management (PCM) with touchscreen, navigation, and smartphone integration."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button19').addEventListener('click', () => {
    const text = "Dual or tri-zone automatic climate control."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button20').addEventListener('click', () => {
    const text = "Front, side, and curtain airbags for safety."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button21').addEventListener('click', () => {
    const text = "Adaptive cruise control, lane-keeping assist, and parking sensors."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button22').addEventListener('click', () => {
    const text = " Anti-lock braking system (ABS) and Porsche Stability Management (PSM)."
    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});

// Handling the button click event
document.getElementById('button23').addEventListener('click', () => {
    const text = "Advanced traction and launch control systems."
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});






// Handling the button click event
document.getElementById('button25').addEventListener('click', () => {
    const text = "he water pump is responsible for circulating coolant through the engine and the water jacket. It ensures that the engine stays cool by moving the coolant through the engine and radiator, where the heat is released. The pump is essential for preventing the engine from getting too hot, especially during long rides.";
    
    // Trigger the voice assistant without showing any text
    speakText(text);
    
});